import React from 'react';
import {StyleSheet, View} from 'react-native';

const HeaderBack = () => {
  return <View style={styles.headerBack} />;
};

const styles = StyleSheet.create({
  headerBack: {
    backgroundColor: '#16171d',
    width: '100%',
    height: '100%',
  },
});

export default HeaderBack;
